// ====================================================== // USER ANSWERS // ====================================================== let userAnswers = {}; // ====================================================== // PAGE ORDER // ====================================================== const pageOrder = [ 'startPage', 'q1', 'q2', 'q3', 'q4', 'q5', 'q6', 'q7', 'results' ]; // ====================================================== // SHOW PAGE // ====================================================== function showPage(id) { pageOrder.forEach(page => { const element = document.getElementById(page); if (element) { element.classList.remove('active'); } }); const targetPage = document.getElementById(id); if (targetPage) { targetPage.classList.add('active'); } } // ====================================================== // START BUTTON // ====================================================== document.addEventListener('DOMContentLoaded', function () { const startButton = document.getElementById('startBtn'); if (startButton) { startButton.addEventListener('click', function () { showPage('q1'); }); } }); // ====================================================== // NEXT QUESTION // ====================================================== function nextQuestion(currentId, answer) { // Save answer userAnswers[currentId] = answer; // Find current page const currentIndex = pageOrder.indexOf(currentId); // Find next page if ( currentIndex >= 0 && currentIndex < pageOrder.length - 1 ) { const nextId = pageOrder[currentIndex + 1]; // Show next page showPage(nextId); // If this is the results page, // calculate and display the result if (nextId === 'results') { displayResults(); } } } // ====================================================== // CALCULATE OUTCOME // ====================================================== function calculateOutcome(answers) { const points = { AP: 0, SO: 0, AF: 0, IU: 0, DG: 0 }; // ------------------------------ // QUESTION 1 // ------------------------------ switch (answers.q1) { case 'Art': points.AP++; points.DG++; break; case 'Politics': points.AF++; break; case 'Entertainment': points.AP++; points.DG++; break; case 'Technology': points.DG++; points.AF++; break; } // ------------------------------ // QUESTION 2 // ------------------------------ switch (answers.q2) { case 'Like': points.AF++; break; case 'Comment': points.AP++; points.AF++; break; case 'Share': points.AP++; points.DG++; break; case 'Scroll': points.SO++; points.IU++; break; } // ------------------------------ // QUESTION 3 // ------------------------------ switch (answers.q3) { case 'A': points.AP++; points.AF++; break; case 'B': points.DG++; break; case 'C': points.AF++; break; } // ------------------------------ // QUESTION 4 // ------------------------------ switch (answers.q4) { case 'A-funny': points.AP++; break; case 'B-serious': points.AF++; break; case 'C-aspiration': points.AP++; points.DG++; break; } // ------------------------------ // QUESTION 5 // ------------------------------ switch (answers.q5) { case 'Yes': points.AP++; points.DG++; break; case 'No': points.AF++; break; case 'Unsure': points.DG++; points.IU++; break; } // ------------------------------ // QUESTION 6 // ------------------------------ switch (answers.q6) { case 'Similar': points.AP++; points.AF++; break; case 'Unexpected': points.DG++; points.IU++; break; } // ------------------------------ // QUESTION 7 // ------------------------------ switch (answers.q7) { case 1: case 2: points.SO++; points.IU++; break; case 3: points.AF++; break; case 4: case 5: points.AP++; points.AF++; break; } // ================================================== // FIND HIGHEST SCORE // ================================================== let maxPoints = 0; let outcome = ''; for (const key in points) { if (points[key] > maxPoints) { maxPoints = points[key]; outcome = key; } } // ================================================== // TIE BREAKER // ================================================== const tieBreakerPriority = [ 'AP', 'DG', 'AF', 'SO', 'IU' ]; const topScores = Object.keys(points).filter( key => points[key] === maxPoints ); if (topScores.length > 1) { for (const key of tieBreakerPriority) { if (topScores.includes(key)) { outcome = key; break; } } } return outcome; } // ====================================================== // DISPLAY RESULTS // ====================================================== function displayResults() { const container = document.getElementById('resultsContainer'); if (!container) { console.error( 'resultsContainer was not found.' ); return; } // Clear old results container.innerHTML = ''; // Calculate result const finalOutcomeKey = calculateOutcome(userAnswers); // ================================================== // OUTCOME IMAGES // ================================================== const outcomeImages = { AP: 'Outcome-A.png', SO: 'Outcome-B.png', AF: 'Outcome-C.png', IU: 'Outcome-D.png', DG: 'Outcome-E.png' }; // ================================================== // WARNING IMAGES // ================================================== const warningImages = { AP: 'Outcome-A-warning.png', SO: 'Outcome-B-warning.png', AF: 'Outcome-C-warning.png', IU: 'Outcome-D-warning.png', DG: 'Outcome-E-warning.png' }; // ================================================== // IMAGE WRAPPER // ================================================== const imageWrapper = document.createElement('div'); imageWrapper.style.display = 'flex'; imageWrapper.style.alignItems = 'center'; imageWrapper.style.justifyContent = 'center'; imageWrapper.style.gap = '30px'; imageWrapper.style.flexWrap = 'wrap'; // ================================================== // OUTCOME IMAGE // ================================================== const img = document.createElement('img'); img.src = outcomeImages[finalOutcomeKey]; img.alt = `Outcome ${finalOutcomeKey}`; img.style.width = '400px'; img.style.height = 'auto'; img.style.cursor = 'pointer'; img.classList.add('outcome-spin'); // ================================================== // CLICKABLE OUTCOME IMAGE // ================================================== const link = document.createElement('a'); link.href = outcomeImages[finalOutcomeKey]; link.target = '_blank'; link.appendChild(img); // ================================================== // WARNING IMAGE // ================================================== const warningImg = document.createElement('img'); warningImg.src = warningImages[finalOutcomeKey]; warningImg.alt = `Warning for Outcome ${finalOutcomeKey}`; warningImg.style.width = '280px'; warningImg.style.height = 'auto'; // ================================================== // ADD IMAGES // ================================================== imageWrapper.appendChild(link); imageWrapper.appendChild(warningImg); // ================================================== // ADD TO RESULTS // ================================================== container.appendChild(imageWrapper); // ================================================== // RESULTS BUTTON // ================================================== const existingButton = document.querySelector('#results button'); if (existingButton) { existingButton.onclick = goToNextSection; } } // ====================================================== // FINAL BUTTON // ====================================================== function goToNextSection() { window.location.href = 'https://isabellegriffiths.my.canva.site/dissertation-website'; }
